{
  "name": "simple-chat",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "nuxt & json-server --watch db.json --port 3001",
    "build": "nuxt build",
    "start": "nuxt start & json-server --watch db.json --port 3001",
    "generate": "nuxt generate"
  },
  "dependencies": {
    "bulma": "^0.9.2",
    "core-js": "^3.8.3",
    "nuxt": "^2.14.12"
  },
  "devDependencies": {
    "fibers": "^5.0.0",
    "sass": "^1.32.8",
    "sass-loader": "^10.1.1"
  }
}
